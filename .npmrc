# NPM Configuration for React Native/Expo project
# Use legacy peer deps for better compatibility with React Native ecosystem
legacy-peer-deps=true

# Automatically save exact versions (optional, for better reproducibility)
save-exact=true

# Prefer offline cached packages when available
prefer-offline=true

# Set registry (uncomment if using private registry)
# registry=https://registry.npmjs.org/